name: outpost-server
arch: "${PKG_ARCH}"
platform: linux
version: "${PKG_VERSION}"
section: net
priority: optional
maintainer: "Mathias Wagner <mathias@gnm.dev>"
description: |
  Outpost server - Bundles web interface together with server
vendor: "Outpost"
homepage: "https://github.com/gnmyt/Nexterm"
license: "MIT"

overrides:
  deb:
    depends:
      - libc6
  rpm:
    depends:
      - glibc

contents:
  - src: stage/server/outpost-server
    dst: /usr/bin/outpost-server
    file_info:
      mode: 0755

  - src: packaging/outpost-server.service
    dst: /lib/systemd/system/outpost-server.service
    file_info:
      mode: 0644

  - src: packaging/server.env
    dst: /etc/outpost-server/server.env
    type: config|noreplace
    file_info:
      mode: 0640

  - dst: /var/lib/outpost-server
    type: dir
    file_info:
      mode: 0750

scripts:
  postinstall: packaging/scripts/server-postinstall.sh
  preremove: packaging/scripts/server-preremove.sh
  postremove: packaging/scripts/server-postremove.sh
